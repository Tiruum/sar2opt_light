albumentations==2.0.8
bitsandbytes>=0.43
colorama==0.4.6
matplotlib==3.10.8
numpy==2.4.2
omegaconf==2.3.0
opencv_python==4.11.0.86
opencv_python_headless==4.11.0.86
optuna==4.5.0
pandas==3.0.1
Pillow==12.1.1
pyTelegramBotAPI==4.31.0
python-dotenv==1.2.1
pytorch_lightning==2.5.5
rasterio==1.4.3
tensorboard>=2.14.0
torch==2.8.0+cu129
torchmetrics==1.6.1
torchvision==0.23.0+cu129
# SAR2OPT-V1 additions (May 2026)
focal-frequency-loss>=0.3.0   # Jiang ICCV 2021, sharp HF anchor (loss term in sar2opt_v1)
pytorch-msssim>=1.0.0          # MS-SSIM (also pulled in transitively by torchmetrics)
kornia>=0.7.0                  # rgb_to_lab path used by LABChromaL1Loss
transformers>=4.41.0           # HF AutoBackbone (SwinV2 / ConvNeXtV2 generators)
lpips>=0.1.4                   # val metric only in sar2opt_v1, training loss in older stacks
psutil>=5.9.0                  # cpu_rss_gb probe in llwt_v45 main.py — disambiguates GPU-alloc-fragmentation vs host-side leak for the per-epoch train-time ramp
# ONNX export (src/models/wavenext/export_onnx.py) — optional, fp32 export + parity
onnx>=1.16.0
onnxruntime>=1.18.0
