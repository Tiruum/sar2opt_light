\documentclass[border=8pt, multi, tikz]{standalone}
\usepackage{import}
\subimport{../layers/}{init}
\usetikzlibrary{positioning}
\usetikzlibrary{3d}
\usetikzlibrary{calc}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[main=russian,english]{babel}

\def\ConvColor{rgb:yellow,5;red,2.5;white,5}
\def\ConvReluColor{rgb:yellow,5;red,5;white,5}
\def\HFColor{rgb:cyan,4;blue,1;white,3}
\def\OutColor{rgb:magenta,5;black,7}

\begin{document}
\begin{tikzpicture}
\tikzstyle{connection}=[ultra thick,
  every node/.style={sloped,allow upside down},
  draw=\edgecolor, opacity=0.7]
\tikzstyle{trunk}=[ultra thick, draw=\edgecolor, opacity=0.7]

%% ══════════════════════════════════════════════════════════════════════
%% Входы
%% ══════════════════════════════════════════════════════════════════════

%% OPT — общий вход (центр)
\pic[shift={(0,0,0)}] at (0,0,0) {Box={
  name=opt,
  caption=ОПТ,
  xlabel={{3, }},
  zlabel=256,
  fill={rgb:green,2;yellow,1;white,2},
  height=22, width=2, depth=22
}};

%% SAR — для основного дискриминатора (верх)
\pic[shift={(0,8,0)}] at (0,0,0) {Box={
  name=sar,
  caption=РЛС,
  xlabel={{1, }},
  zlabel=256,
  fill={rgb:blue,1;black,0.3},
  height=22, width=1.5, depth=22
}};

%% ══════════════════════════════════════════════════════════════════════
%% MainDis  (верхняя ветка, y = 8) — условный многомасштабный PatchGAN
%% ══════════════════════════════════════════════════════════════════════

\pic[shift={(1.7,8,0)}] at (0,0,0) {Box={
  name=mcat,
  caption={Concat\\РЛС+ОПТ},
  xlabel={{4, }},
  zlabel=256,
  fill=\ConvColor,
  height=22, width=2, depth=22
}};
\draw[connection] (sar-east) -- node {\midarrow} (mcat-west);

\pic[shift={(1.5,0,0)}] at (mcat-east) {RightBandedBox={
  name=mconv,
  caption={4×Conv\\LeakyReLU},
  xlabel={{64, 128, 256, 512}},
  zlabel=30,
  fill=\ConvColor,
  bandfill=\ConvReluColor,
  height=8, width={3,3,4,5}, depth=8
}};
\draw[connection] (mcat-east) -- node {\midarrow} (mconv-west);

\pic[shift={(2,2,0)}] at (mconv-east) {Box={
  name=m70,
  caption={logits\\70×70},
  xlabel={{1, }},
  zlabel=70,
  fill=\OutColor,
  opacity=0.7,
  height=12, width=2, depth=12
}};
\draw[connection] (mconv-east) -- node {\midarrow} (m70-west);

\pic[shift={(2,-2,0)}] at (mconv-east) {Box={
  name=m46,
  caption={logits\\46×46},
  xlabel={{1, }},
  zlabel=46,
  fill=\OutColor,
  opacity=0.7,
  height=8, width=2, depth=8
}};
\draw[connection] (mconv-east) -- node {\midarrow} (m46-west);

%% ══════════════════════════════════════════════════════════════════════
%% HF-D  (нижняя ветка, y = -8) — условный SN-PatchGAN на ВЧ-остатке оптики
%% ══════════════════════════════════════════════════════════════════════

%% Высокочастотный остаток оптики: h(x) = x - G_sigma * x  (sigma=2)
\pic[shift={(1.7,0,0)}] at (0,0,0) {Box={
  name=hp,
  caption={ВЧ-остаток\\$x{-}G_\sigma{*}x$\\$\sigma{=}2$},
  xlabel={{3, }},
  zlabel=256,
  fill=\HFColor,
  height=22, width=2, depth=22
}};

\pic[shift={(3.6,0,0)}] at (0,0,0) {Box={
  name=hcat,
  caption={Concat\\РЛС+ВЧ},
  xlabel={{4, }},
  zlabel=256,
  fill=\ConvColor,
  height=22, width=2, depth=22
}};
\draw[connection] (hp-east)   -- node {\midarrow} (hcat-west);
\draw[connection] (sar-east) -- ++(0,-4,0) -| node {\midarrow} (hcat-west);

\pic[shift={(1.5,0,0)}] at (hcat-east) {RightBandedBox={
  name=hconv,
  caption={4×Conv\\SN LeakyReLU},
  xlabel={{64, 128, 256, 512}},
  zlabel=30,
  fill=\ConvColor,
  bandfill=\ConvReluColor,
  height=8, width={3,3,4,5}, depth=8
}};
\draw[connection] (hcat-east) -- node {\midarrow} (hconv-west);

\pic[shift={(2,0,0)}] at (hconv-east) {Box={
  name=hout,
  caption={logits\\70×70},
  xlabel={{1, }},
  zlabel=70,
  fill=\OutColor,
  opacity=0.7,
  height=12, width=2, depth=12
}};
\draw[connection] (hconv-east) -- node {\midarrow} (hout-west);
%% ══════════════════════════════════════════════════════════════════════
%% Развилка OPT: вверх (MainDis) + вниз (HF-D через ВЧ-остаток)
%% ══════════════════════════════════════════════════════════════════════
\coordinate (Dfork)     at (1.2,  0);
\coordinate (DforkUp)   at (1.2,  8);
\coordinate (DforkDown) at (1.2, 0);

\draw[trunk] (opt-east) -- (Dfork);
\draw[trunk] (DforkDown) -- (DforkUp);
\draw[trunk, -Stealth] (DforkUp)   -- (mcat-west);
\draw[trunk, -Stealth] (DforkDown) -- (hp-west);

\filldraw[fill=\edgecolor, draw=\edgecolor, opacity=0.9] (Dfork) circle (2.5pt);

\end{tikzpicture}
\end{document}