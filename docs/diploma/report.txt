================================================================================
ОТЧЁТ О РЕЦЕНЗИРОВАНИИ ВКР МАГИСТРА
"Использование генеративных нейронных сетей для трансфера изображений
радиолокационного диапазона в оптический"
Автор: Селин Тимур Александрович, МФТИ, ФАКТ, каф. ВФ
Дата рецензирования: 2026-05-23
================================================================================


────────────────────────────────────────────────────────────────────────────────
0. ИТОГОВАЯ СВОДКА (TL;DR)
────────────────────────────────────────────────────────────────────────────────

СОСТОЯНИЕ:           критическое. До защиты работа не готова.
Объём:               57 страниц из 60 разрешённых (3 страницы запаса).
Главы 6 и 7 пусты:   "Результаты" содержит ТОЛЬКО заголовок
                     "Визуализация данных reactingFoam" — это шаблонный
                     остаток от OpenFOAM (CFD), не имеющий никакого
                     отношения к работе. "Заключение" пуста полностью.
Главный вклад:       заявлен как "улучшение существующих моделей и
                     разработка собственных" (глава 5). Глава занимает
                     11 страниц, но критически не хватает количественных
                     результатов: ни одной сравнительной таблицы, ни одной
                     ablation-таблицы, метрики только у одной из трёх
                     собственных моделей. Подраздел 5.3.2 "Лучшая
                     собственная модель" — полностью пустой.

ТРИ ГЛАВНЫЕ ПРОБЛЕМЫ:

  (1) ПРОПОРЦИИ. Фон + методы + обзор = 32 стр. (56% работы); собственный
      вклад = 11 стр. Магистерская работа с заявленным вкладом по
      моделям должна иметь обратное соотношение. Необходимо сжать
      главы 2–4 на 8–10 страниц, чтобы освободить место под результаты,
      заключение и расширение вклада.

  (2) ОТСУТСТВУЕТ ОСНОВНОЕ. Главы "Результаты" и "Заключение" пусты;
      подраздел "Лучшая собственная модель" пуст; ни одной сравнительной
      таблицы; пустая figure-обёртка fig:lightweight_arch (нет
      \includegraphics).

  (3) БИБЛИОГРАФИЯ. Ссылка chu2017cyclegan указывает на работу
      "CycleGAN, a master of steganography" — это НЕ оригинальная статья
      CycleGAN (Zhu et al. 2017). В references.bib стоит комментарий
      "% Проверить ниже" (строка 161). Несколько ссылок 2025 года
      требуют верификации. В сравнительной таблице §4.5 ни одна модель
      не имеет цитирования.

ОБЪЁМ РАБОТЫ (страницы по main.toc):

  Аннотация                                    2–3   (1–2 стр.)
  Глава 1: Введение                            4–7   (4 стр.)
  Глава 2: Физика SAR/Optical                  8–14  (7 стр.)
  Глава 3: Материалы и методы                  15–27 (13 стр.)  ← крупнейшая
  Глава 4: Обзор существующих подходов         28–39 (12 стр.)
  Глава 5: Собственный вклад                   40–50 (11 стр.)  ← главное
  Глава 6: Результаты                          51    (1 стр., пуста)
  Глава 7: Заключение                          52    (1 стр., пуста)
  Библиография                                 53–55
  Приложение                                   56–57

ЦЕЛЕВОЕ РАСПРЕДЕЛЕНИЕ ПОСЛЕ СЖАТИЯ (60 стр. лимит):

  Аннотация                                    1–2
  Глава 1: Введение                            3–4 (сжать на 1 стр.)
  Глава 2: Физика                              3–4 (сжать на 3 стр.)
  Глава 3: Материалы и методы                  7–8 (сжать на 5 стр.)
  Глава 4: Обзор подходов                      7   (сжать на 5 стр.)
  Глава 5: Собственный вклад                   12–14 (расширить на 1–3)
  Глава 6: Результаты                          4–5 (написать)
  Глава 7: Заключение                          2   (написать)
  Библиография                                 2
  Приложение                                   1–2
  ────────────────────────────────────────────
  Итого:                                       42–48 + ~10 (приложение/
                                               библиография) ≈ 55–58


────────────────────────────────────────────────────────────────────────────────
1. АННОТАЦИЯ (parts/Annotation.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    22 строки, ~1 стр.
Качество: УДОВЛЕТВОРИТЕЛЬНОЕ, требует обновления к финальной версии.

Замечания:
  • В аннотации заявлены ТОЛЬКО Pix2Pix и CFRWD-GAN; не упомянуты ни
    собственные модели, ни облегчённая иерархическая архитектура с
    ConvNeXtV2, ни AdaptiveFusion. Это несовместимо с заявленным
    "основным вкладом" работы.
  • Ключевые слова не содержат: "вейвлет-преобразование", "механизм
    внимания", "CFRWD", "ConvNeXtV2" — а это ключевые компоненты
    реализованных моделей.
  • В строках 17–19 — закомментированный пустой английский Abstract.
    Если по требованиям МФТИ английский абстракт нужен — заполнить;
    если нет — удалить.
  • Грамматика и стиль — без замечаний.

Рекомендация: после переработки глав 5–7 переписать аннотацию,
добавив 2–3 предложения о собственных модификациях и их результатах.


────────────────────────────────────────────────────────────────────────────────
2. ГЛАВА 1 — ВВЕДЕНИЕ (parts/Chapter0.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    75 строк, 4 страницы.
Качество: ХОРОШЕЕ. Структура соответствует требованиям ВКР МФТИ.

Сильные стороны:
  • Актуальность и степень разработанности изложены чётко.
  • Цели и задачи сформулированы операционально (5 пунктов).
  • Имеется упоминание апробации (68-я ВНК МФТИ).

Замечания:
  • Строка 30: "сильно уступая в скорости как обучения и вывода (инференс),
    так и в затрачиваемых ресурсах" — стилистически тяжёлая конструкция,
    повторяет "как ... так и" без необходимости.
  • Строка 31: предложение об ускорении диффузионных моделей
    методами дистилляции должно либо иметь ссылку, либо быть удалено
    (тангенциальное).
  • Раздел "Научная новизна" (стр. 6) сформулирован слишком общо:
    "адаптации существующих моделей ... улучшении ... разработке". Нужно
    конкретизировать (например: "впервые предложен механизм
    пространственно-адаптивного слияния CFR/WD-ветвей через
    softmax-карту весов; разработана легковесная иерархическая
    архитектура на замороженном ConvNeXtV2 для SAR-to-Optical").
  • Раздел "Методология": упомянуты PyTorch и Lightning; стоит
    добавить, на каких GPU проводились эксперименты, если глава 5
    об этом подробно говорит.

Сжатие: возможно ~0.5 стр. экономии за счёт объединения разделов
"Объект и предмет исследования" + "Научная новизна" + "Теоретическая
и практическая значимость" в один компактный блок, как часто делается
в новых редакциях ГОСТ-стиля.

Рекомендуемая целевая длина: 3–3.5 страницы.


────────────────────────────────────────────────────────────────────────────────
3. ГЛАВА 2 — ТЕОРЕТИЧЕСКИЕ ОСНОВЫ (parts/Chapter1.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    206 строк, 7 страниц.
Качество: написана грамотно, физически в основном корректна,
          но избыточна для магистерской по ML.

ФАКТИЧЕСКИЕ ОШИБКИ:
  • Стр. 11 (городская застройка): причиной яркости названо "доминирование
    лейовера". Это путаница двух эффектов: яркость дают двугранные
    угловые отражатели (corner reflection), лейовер — геометрическое
    искажение. Требуется правка.
  • Уравнение переноса излучения: L_рассеян названо "атмосферной дымкой" —
    разговорный термин; правильнее "путевое рассеяние" (path radiance).
  • Формула R_r = c/(2B) корректна, но без оговорки "после сжатия
    ЛЧМ-импульса" неточна.
  • Утверждение об отсутствии линейного оператора L: L(Σ) = ρ
    подаётся без обоснования — либо обосновать, либо убрать.

ТЕКСТ:
  • "Форшотенинг" — заменить на русский термин "укорочение" или
    использовать "сжатие проекции склонов".
  • "Лэйовер" → "лейовер" (или "наложение").
  • Строка 205: опечатка "отсустствия" → "отсутствия".
  • Терминологический разнобой: РЛИ / SAR-снимок / РСА / радар —
    выбрать одно и придерживаться.

ИЗБЫТОЧНОСТЬ (с другими главами):
  • Секция 2.4 (Характеристики данных как входных сигналов) дублирует
    то, что должно быть в главе "Материалы и методы". Сам автор
    ссылается на главу методов из этого раздела.
  • Таблица "сравнение SAR/Optical" дублирует нумерованные списки в
    секциях 2.1 и 2.2.
  • 4 отдельных рисунка по типам покрытия (вода/город/трава/горы) — то
    же сравнение, сделанное трижды разными способами.

КОНКРЕТНЫЕ ЦЕЛИ СЖАТИЯ (с экономией страниц):
  ┌────────────────────────────────────────────────────┬─────────┐
  │ Что урезать                                        │ Экономия│
  ├────────────────────────────────────────────────────┼─────────┤
  │ Дубль-списки SAR/Optical (стр. 26–36, 46–50)       │ ~0.5 стр│
  │ 4 рисунка → 1 сводная фигура 2×4                   │ ~1.5 стр│
  │ Секция 2.4 → перенос в гл.3 или сжатие до абзаца   │ ~0.5 стр│
  │ Уравнение переноса излучения → 1 предложение       │ ~0.2 стр│
  │ Формальное отображение G → перенос в гл.3          │ ~0.1 стр│
  ├────────────────────────────────────────────────────┼─────────┤
  │ ИТОГО                                              │ ~2.5–3  │
  └────────────────────────────────────────────────────┴─────────┘

ВЕРДИКТ: сжать с 7 до 3.5–4 страниц. Это даст 3 страницы под
результаты/заключение.


────────────────────────────────────────────────────────────────────────────────
4. ГЛАВА 3 — МАТЕРИАЛЫ И МЕТОДЫ (parts/Chapter2.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    461 строка, 13 страниц. ГЛАВНЫЙ ОБЪЕКТ СЖАТИЯ.
Качество: формулы метрик в основном корректны, но глава перегружена
          текстом, который повторяется и/или относится к другим главам.

МАТЕМАТИЧЕСКИЕ ЗАМЕЧАНИЯ:
  • PSNR: ссылается на MSE, но MSE не определена в тексте (введена
    только RMSE). Записать формулу через RMSE или ввести MSE.
  • SSIM: утверждение о диапазоне [-1, 1] технически верно, но для
    изображений практически [0, 1]. Две строки разъяснений о SSIM=-1
    можно убрать.
  • FDE/Frequency Loss: индексы суммирования $\sum_{u=1}^H$ vs.
    $\sum_{u,v}$ без границ — привести к единой записи.
  • LPIPS: в формуле опущена нормализация признаков по каналам
    $\hat{\phi}$ — методически важная деталь из Zhang et al. 2018.
  • SAMScore: цитируется только SAM-модель (kirillov2023segment), но
    не первоисточник самой метрики SAMScore. Если это собственный
    предложенный показатель — указать явно как вклад работы.
  • DINOv2 perceptual loss: процитирован только сам DINOv2; нет ссылки
    на работу, использующую DINOv2 как perceptual loss. Если это
    собственная конструкция — заявить явно.

ОПЕЧАТКИ И ГРАММАТИКА:
  • стр. 308: "перемещаение" → "перемещение".
  • стр. 413: "тектуры" → "текстуры".
  • стр. 230 (в таблице): "Попексельное" → "Попиксельное".
  • стр. 344–345: висячее придаточное со словом "которые".

СТРУКТУРНЫЕ ПРОБЛЕМЫ:
  • §3.1 (Формальная постановка) — всего 3 предложения, не дотягивает
    до самостоятельной секции. Включить как первый абзац §3.2.
  • Подраздел "Дифференцируемость компонент" (~1.5 стр.) — констатирует
    очевидное (все PyTorch-функции потерь дифференцируемы). К работе
    научного содержания не добавляет.
  • Сводная таблица метрик/потерь (стр. 207–238) повторяет всё
    содержание предшествующего текста. Кандидат на перенос в приложение.
  • §3.4 "Выводы по третьей главе" дословно перефразирует введение
    к главе.
  • Описание физики сенсоров SEN1-2 и QXS-SAROPT (полоса C, IW-режим,
    L2A-коррекция) дублирует главу 2.
  • Аугментации описаны на ~1.5 стр. — раздуто для стандартных приёмов.

КОНКРЕТНЫЕ ЦЕЛИ СЖАТИЯ:
  ┌─────────────────────────────────────────────────────┬──────────┐
  │ Что урезать                                         │ Экономия │
  ├─────────────────────────────────────────────────────┼──────────┤
  │ §3.1 → 1 абзац внутри §3.2                          │ ~0.1 стр │
  │ RMSE/PSNR в один абзац с одной формулой             │ ~0.4 стр │
  │ SSIM комментарий о [-1,1]                           │ ~0.1 стр │
  │ "Дифференцируемость компонент" → удалить            │ ~1.0 стр │
  │ Сводная таблица → в приложение                      │ ~1.0 стр │
  │ Физика сенсоров SEN1-2 → в гл.2 или сжать           │ ~0.5 стр │
  │ Структура/лицензия SEN1-2 → 1 предложение           │ ~0.1 стр │
  │ Стохастические аугментации → булет-список           │ ~0.7 стр │
  │ Физика сенсоров QXS-SAROPT → сжать                  │ ~0.4 стр │
  │ Сравнительное описание датасетов                    │ ~0.4 стр │
  │ §3.4 "Выводы" → 1 предложение или удалить           │ ~0.3 стр │
  ├─────────────────────────────────────────────────────┼──────────┤
  │ ИТОГО                                               │ ~5 стр   │
  └─────────────────────────────────────────────────────┴──────────┘

ЧЕГО НЕДОСТАЁТ:
  • Точные числа train/test для SEN1-2 (заявлено 4:1, абсолютные
    значения не указаны).
  • Абсолютные числа train/test для QXS-SAROPT (16000/4000).
  • Конкретные значения весов λ в полной целевой функции (6 весов).
  • Какой backbone у LPIPS используется (AlexNet/VGG/Squeeze) —
    окончательно, а не "чаще всего".

ВЕРДИКТ: сжать с 13 до 7–8 страниц. Освобождает 5–6 страниц.


────────────────────────────────────────────────────────────────────────────────
5. ГЛАВА 4 — ОБЗОР СУЩЕСТВУЮЩИХ ПОДХОДОВ (parts/Chapter3.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    259 строк, 12 страниц.
Качество: широкий охват, но местами сваливается в чистый survey
          без связи с работой; критические проблемы с ссылками.

ОШИБКИ ССЫЛОК (КРИТИЧНО):
  • Стр. 29: \cite{cciccek20163d} для U-Net — это 3D U-Net (Çiçek et al.,
    2016, медицинская сегментация), а не оригинальная статья
    Ronneberger et al. 2015. Заменить на корректную ссылку
    (добавить запись @inproceedings{ronneberger2015u,...} в bib).
  • Стр. 47: "(cGAN loss)~\ref{eq:cgan_loss}" и "L1-норма~\ref{eq:l1_loss}"
    — ссылки \ref на ярлыки уравнений, которые НИГДЕ не определены.
    В скомпилированном PDF будет "??". Либо ввести уравнения с
    метками, либо убрать \ref.
  • Стр. 20: "($C_{in}$ обычно равно 1 для одноканального SAR в формате
    RGB)" — логическая несовместимость: одноканальный SAR не может
    быть в формате RGB.
  • Сравнительная таблица §4.5: упомянутые модели INRECGAN, ScE-GAN,
    DTGAN, DDA-GAN, S20CDM, E³Diff БЕЗ единого \cite. Это блокирующая
    проблема: ни одна цифра в таблице не верифицируема. Добавить
    цитирования к каждой строке.
  • ТАБЛИЦА §4.5 НЕ СОДЕРЖИТ CFRWD-GAN — основной baseline, который
    адаптирует работа.

ВЕРОЯТНО ПРИДУМАННЫЕ ССЫЛКИ (требуют верификации):
  - george2022wavelet, zhang2025seg, zhao2025hvt, wei2025sequential,
    bralet2024dem, zhang2024interpretable — проверить существование
    публикаций. Особенно подозрительны 2025-е (граница знаний модели).
  - henry2021pix2pix (в bib): журнал "Research Gate Publication" — не
    реальная площадка. Заменить на исходную статью Isola et al. 2017
    (isola2017image уже есть в bib).

ТЕКСТ:
  • стр. 63: "трудности с сходимостью" → "трудности со сходимостью".
  • "reasoning" неперевод (стр. 110, 113, 143) рядом с "рассуждение"
    (стр. 106) — выбрать.
  • Двойные заключительные абзацы §4.2 (стр. 98–99) повторяют введение
    к секции.
  • §4.3 заканчивается абзацем (стр. 181), повторяющим её начало.
  • стр. 225: "память-ёмкость" — заменить на "требовательность к памяти".

ИЗБЫТОЧНОСТЬ:
  • §4.3 содержит ДВЕ полноразмерные фигуры (fig:cfrwd_arch + fig:cfr_arch)
    + отдельную фигуру fig:wd + полные уравнения CFR. Глава 5 (вклад)
    переописывает то же. Оставить здесь сжатое описание + одну фигуру;
    либо вынести подробную архитектуру в гл. 5, а здесь только концепт.
  • §4.4 (трансформеры) — ~1.5–2 стр. Если в работе НЕ реализована и
    НЕ сравнивается ни одна трансформерная модель — секция избыточна
    как чистый survey. Если используется (вероятно нет) — оставить
    1 абзац с целевой моделью.
  • стр. 93–94 переопределяют спекл/layover/shadowing/foreshortening —
    уже определены в гл. 2. Заменить на back-reference.

ЦЕЛИ СЖАТИЯ:
  ┌─────────────────────────────────────────────────────┬──────────┐
  │ Что урезать                                         │ Экономия │
  ├─────────────────────────────────────────────────────┼──────────┤
  │ fig:pix2pix_example (общие примеры Pix2Pix)         │ ~0.3 стр │
  │ Объединить fig:unet_arch + fig:patchGAN или убрать  │ ~0.6 стр │
  │ §4.1.2 enumerate ограничений Pix2Pix                │ ~0.5 стр │
  │ Сжать CFRWD-фигуры до одной                         │ ~0.8 стр │
  │ Убрать fig:wd, описание в тексте                    │ ~0.3 стр │
  │ §4.4 трансформеры — удалить или сжать до абзаца     │ ~1.5–2.0 │
  │ Bullet-список физических априорных данных           │ ~0.5 стр │
  │ Закрывающие абзацы §4.2                             │ ~0.2 стр │
  ├─────────────────────────────────────────────────────┼──────────┤
  │ ИТОГО                                               │ ~4.5–5   │
  └─────────────────────────────────────────────────────┴──────────┘

ЧЕГО НЕДОСТАЁТ:
  • CFRWD-GAN отсутствует в сравнительной таблице — обязательно
    добавить (это baseline работы).
  • SEN-12 и QXS-SAROPT в шапке таблицы названы, но не описаны на
    этой странице (описание в гл. 3, но требуется хотя бы 1 предложение
    или back-reference).
  • Метрики PSNR/SSIM/LPIPS/FID в таблице без введения — либо короткое
    напоминание, либо back-reference к гл. 3.
  • Диффузионные модели (S20CDM, E³Diff) в таблице, но не обсуждаются
    в тексте. Либо убрать из таблицы, либо добавить 1 абзац.

ВЕРДИКТ: сжать с 12 до 7 страниц. Освобождает 5 страниц.


────────────────────────────────────────────────────────────────────────────────
6. ГЛАВА 5 — СОБСТВЕННЫЙ ВКЛАД (parts/Chapter4.tex)  ← ГЛАВНОЕ
────────────────────────────────────────────────────────────────────────────────

Объём:    223 строки, 11 страниц.
Качество: ОПИСАТЕЛЬНОЕ, но катастрофически не хватает доказательной
          базы. Заявленный "главный вклад" работы в существующем виде
          не обоснован количественно.

ПУСТОЕ И ПОЛОМАННОЕ:
  • Подраздел 5.3.2 "Лучшая собственная модель" (стр. 221–223) —
    ПОЛНОСТЬЮ ПУСТ, только заголовок и \newpage. Это заявленная
    кульминация работы.
  • fig:lightweight_arch (стр. 206–210) — figure-обёртка БЕЗ
    \includegraphics. В PDF — пустое место с подписью.
  • Дублирование \label{subsec:lightweight_hierarchical_model}
    (строки 202 и 222) — приведёт к LaTeX warning.

ОТСУТСТВИЕ ДОКАЗАТЕЛЬНОЙ БАЗЫ (главная проблема всей работы):

  Модифицированный Pix2Pix:
    • Описаны 8 модификаций (Norm, Bilinear+Conv, projection skip,
      CBAM+SelfAttention, σ=0.005 init, semantic map, SpectralNorm,
      SelfAttention в дискриминаторе).
    • Quantitative результаты: НОЛЬ метрик. Никакой таблицы.
      Никакого PSNR/SSIM/LPIPS/FID. Только одна качественная
      картинка fig:pix2pix_sar2opt_labeled_200ep.
    • Сам автор пишет (стр. 69–73): модель "продолжает показывать"
      размытие и цветовой дрейф. То есть результат не достигнут,
      но и не оценён численно.
    • Семантическая карта (стр. 52) — крупное архитектурное
      изменение в одном предложении: что за карта, чем
      сегментирована, как объединяется с SAR — не сказано.

  Архитектура, вдохновлённая CFRWD:
    • Описаны: AdaptiveFusion, независимые потоки в частотной
      ветви, CBAM по потокам, two-scale discriminator, AdaptiveLoss.
    • Quantitative результаты: ТОЖЕ НОЛЬ метрик.
    • Только качественные fig:epoch_270 + Приложение.
    • Стр. 135: открыто признаётся "прямое воспроизведение
      оригинальной конфигурации CFRWD-GAN... не представлялось
      возможным" — то есть базовый baseline не получен. Это
      серьёзная методическая проблема: на чём основано утверждение
      об улучшении?

  Облегчённая иерархическая модель (ConvNeXtV2):
    • Единственная модель с цифрами:
        PSNR=19.24 дБ, SSIM=0.461, LPIPS=0.258, FID=344.0
    • НИ ОДНОГО baseline-числа рядом. С чем сравнивать? FID=344 —
      высокий показатель; автор пытается это объяснить (ImageNet↔SAR
      domain shift), но без сравнительной таблицы нельзя оценить.
    • Архитектура описана только на уровне "каскад ConvUpsampleBlock"
      — нет размеров каналов, нет числа блоков, нет лестницы
      разрешений.

ТЕКСТ И ОФОРМЛЕНИЕ:
  • Стр. 52: "снижающих степень некорректности" — дангл-причастие;
    подлежащее не согласовано.
  • Стр. 217: "FID остаётся elevated" — англицизм в русском тексте.
    Также: "ill-posed", "ImageNet↔SAR" code-switching — заменить
    на русские термины ("повышенным", "некорректно поставленная",
    "переход ImageNet→SAR").
  • Стр. 163–168: bullet-список описывает элементы фигуры — это
    содержание подписи (caption), а не основного текста.
  • Стр. 196: "коррелируют с качественным анализом визуальных
    результатов" — циклическое рассуждение (качественный анализ
    подтверждается качественным анализом). Заменить на конкретное
    утверждение со ссылкой на численные показатели.
  • §5.2.1 "Методологический итог" (стр. 172–197) — 26 строк
    summary, которые в значительной мере дословно повторяют
    предшествующие абзацы.

ОЦЕНКА ОРИГИНАЛЬНОСТИ:

  Защитимые оригинальные вклады:
    1. AdaptiveFusion: замена скалярного α в CFRWD на пространственную
       softmax-карту — конкретное, нетривиальное архитектурное
       решение с уравнением. Самый сильный вклад.
    2. Независимые потоки в частотной ветви (LH/HL/HH на двух
       масштабах) — обоснованный выбор с физической мотивацией.
    3. ChannelAdapter для ConvNeXtV2 (1-кан. SAR → 3-кан. вход) —
       стандартный приём, но конкретная реализация может быть
       вкладом.

  Стандартные перекомбинации (НЕ являются вкладом):
    - BatchNorm → InstanceNorm (с 2018, pix2pix-HD).
    - ConvTranspose2d → bilinear+conv (Odena et al. 2016).
    - Spectral Norm (SNGAN 2018).
    - CBAM (Woo et al. 2018, цитируется).
    - AdaptiveLoss / uncertainty weighting (Kendall et al. 2018,
      цитируется).
    - SelfAttention в bottleneck (SAGAN, Attention-Unet).

  Итог: 2–3 защитимых вклада + грамотная инженерная сборка. Для
  магистерской это приемлемо, ЕСЛИ есть численные результаты,
  показывающие улучшение. Сейчас результатов нет.

ЧТО ОБЯЗАТЕЛЬНО ДОБАВИТЬ:
  (1) Ablation-таблица для Pix2Pix: Base / +InstanceNorm /
      +Bilinear / +CBAM / +SpectralNorm / Full — с PSNR/SSIM/LPIPS/FID.
  (2) Метрики для CFRWD-inspired (хотя бы PSNR/SSIM/LPIPS/FID).
  (3) Сводная сравнительная таблица: оригинальный Pix2Pix /
      модифицированный Pix2Pix / CFRWD-GAN (если воспроизведён) /
      CFRWD-inspired / ConvNeXtV2-light.
  (4) Заполнить §5.3.2 "Лучшая собственная модель" (1–1.5 стр.):
      архитектура, обучение, гиперпараметры, размер модели.
  (5) Вставить \includegraphics в fig:lightweight_arch.
  (6) Числа скорости inference (FPS/мс на изображение) для
      обоснования "вычислительных компромиссов" из §5.1.
  (7) Физическое обоснование для частотного внимания: SAR-спекл
    мультипликативный (а не аддитивный, как оптический шум) —
    это и оправдывает вейвлеты + частотный CBAM.

СЖАТИЕ В ЭТОЙ ГЛАВЕ (за счёт перераспределения):
  • §5.2.1 "Методологический итог" → сжать до 8–10 строк (~0.5 стр).
  • Bullet-разбор fig:epoch_270 (стр. 163–168) → в caption (~0.3 стр).
  • Дублирующее описание ограничений Pix2Pix (стр. 21–23) → 1 строка
    back-reference на гл. 4 (~0.2 стр).
  • Дублирующее описание скалярного α CFRWD (стр. 83–88) → 1 строка
    + back-reference (~0.3 стр).
  ────────────────────────────────────
  Экономия: ~1.3 стр. — освобождается под расширение и §5.3.2.

ВЕРДИКТ: глава должна вырасти содержательно (таблицы, числа,
заполнение §5.3.2), но не по объёму. Целевой объём: 12–14 страниц
с интегрированными результатами. Если глава 6 (Результаты) будет
полноценной — то этой главе достаточно 11–12 стр., но с таблицами
вместо повторяющегося текста.


────────────────────────────────────────────────────────────────────────────────
7. ГЛАВА 6 — РЕЗУЛЬТАТЫ И АНАЛИЗ (parts/Chapter5.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    4 строки. ФАКТИЧЕСКИ ПУСТАЯ.

Содержимое целиком:
    \chapter{Результаты и анализ}
    \label{sec:Chapter5} \index{Chapter5}
    \section{Визуализация данных reactingFoam}
    \newpage

reactingFoam — solver OpenFOAM для реагирующих течений (CFD). Это
шаблонный остаток от другой работы, никакого отношения к SAR-to-Optical
не имеет. УДАЛИТЬ заголовок.

ЧТО ДОЛЖНО БЫТЬ В ЭТОЙ ГЛАВЕ (4–5 страниц):
  1. Постановка эксперимента (0.5 стр.):
     - Аппаратное окружение (RTX 4080 Super, 16 ГБ — уже в §5.1).
     - Размер batch, learning rate, число эпох, число train/val
       примеров для каждого датасета.
     - Какие именно реализации сравниваются.

  2. Сводная сравнительная таблица (0.5 стр.):
     - Столбцы: PSNR↑, SSIM↑, LPIPS↓, FID↓, SAMScore (если посчитан),
       параметры модели (M), время инференса (мс).
     - Строки: оригинальный Pix2Pix (если воспроизведён),
       модифицированный Pix2Pix, CFRWD-inspired,
       облегчённая ConvNeXtV2-модель, "лучшая собственная" модель.
     - Жирным выделить лучшие значения.

  3. Ablation-исследование (1 стр.):
     - Для самой важной модификации (AdaptiveFusion в CFRWD-inspired,
       или совокупный набор для Pix2Pix) — таблица с поэтапным
       включением модификаций.

  4. Качественный анализ (1–1.5 стр.):
     - 2–3 рисунка с сравнением выходов разных моделей на одних и
       тех же входных SAR. Уже есть material в Приложении и
       images/cfrwd/output/val.
     - 1 рисунок-failure case (где модели ошибаются), с разбором.

  5. Обсуждение (0.5–1 стр.):
     - Какие модели на каких сценах работают лучше (физическая
       интерпретация: водные поверхности, городская застройка,
       растительность).
     - Compute trade-off: FID↓ vs. размер модели.
     - Ограничения текущей реализации.

  6. Сравнение с литературными baseline (если возможно, 0.5 стр.):
     - Сослаться на таблицу §4.5 (если будет переработана), сравнить
       свою лучшую модель с цифрами из литературы.

КЛЮЧЕВОЕ ТРЕБОВАНИЕ: даже если численные эксперименты не доведены до
полного цикла — необходимо представить ХОТЯ БЫ ОДНУ сравнительную
таблицу. Без этого работа не защищает заявленный вклад.


────────────────────────────────────────────────────────────────────────────────
8. ГЛАВА 7 — ЗАКЛЮЧЕНИЕ (parts/Chapter6.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    3 строки. ПУСТАЯ.

ЧТО ДОЛЖНО БЫТЬ (1.5–2 страницы):

  1. Краткое резюме задачи (3–5 предложений).
  2. Перечисление выполненных задач (зеркало "Задач" из введения):
     - Обзор существующих моделей: какие именно изучены.
     - Подготовка данных: SEN1-2 и QXS-SAROPT, объёмы, аугментации.
     - Разработана модификация Pix2Pix: какие изменения, итог.
     - Разработана модель, вдохновлённая CFRWD: какие изменения, итог.
     - Разработана облегчённая модель на ConvNeXtV2: итог.
     - Экспериментальное исследование: какие метрики, какой выигрыш.
  3. Основные результаты в цифрах (1 абзац):
     - "Лучшая модель достигла PSNR=X.X, SSIM=X.XX на SEN1-2",
       аналогично для QXS-SAROPT.
  4. Научная новизна (2–3 предложения, конкретно):
     - AdaptiveFusion для CFR/WD-слияния.
     - Независимые частотные потоки.
     - ChannelAdapter+frozen ConvNeXtV2 для SAR.
  5. Практическая значимость (2–3 предложения).
  6. Направления дальнейших исследований (3–5 строк):
     - Применение диффузионных моделей.
     - Двунаправленный SAR↔Optical.
     - Multi-temporal данные.
     - Обучение без парных данных через CycleGAN-стиль.

ВНИМАНИЕ: на странице 52 главы 7 нет содержимого, но в TOC
библиография занимает страницы 53–55. Между ними никаких "потерянных"
страниц нет — это просто пустые строки от \newpage в текущих пустых
главах. После заполнения главы 7 объём вырастет естественным образом.


────────────────────────────────────────────────────────────────────────────────
9. ПРИЛОЖЕНИЕ (parts/Appendix.tex)
────────────────────────────────────────────────────────────────────────────────

Объём:    18 строк, 1–2 страницы.
Качество: ПРИЕМЛЕМОЕ. Содержит две сводные фигуры выходов
          CFRWD-inspired модели на валидации.

Замечания:
  • Это место для сводных таблиц и развёрнутых сравнений, которые
    не помещаются в основной текст. Если "сводная таблица метрик/
    потерь" из главы 3 переносится сюда — добавить её.
  • Подписи к figure-ам можно расширить, объяснив, что показано в
    каждом столбце (текущая подпись говорит о "строках сверху вниз",
    но колонки тоже стоит описать, если они нумеруются).
  • Стоит добавить раздел "Дополнительные примеры на QXS-SAROPT"
    параллельно с SEN1-2, если есть результаты.


────────────────────────────────────────────────────────────────────────────────
10. СКВОЗНЫЕ ПРОБЛЕМЫ (references.bib, LaTeX)
────────────────────────────────────────────────────────────────────────────────

БИБЛИОГРАФИЯ (references.bib):

  • Строка 161: комментарий "% Проверить ниже" — ВСЕ ссылки ниже
    этой отметки автор сам пометил неверифицированными. Это:
    woo2023convnext (ConvNeXt V2) — реален, проверен (CVPR 2023).
    ho2020denoising (DDPM) — реален.
    rombach2022high (Latent Diffusion) — реален.
    song2020score — реален.
    ho2022classifier (CFG) — реален.
    liu2022convnet (ConvNeXt) — реален.
    oquab2023dinov2 — реален.
    → Комментарий можно убрать; все ссылки ниже корректны.

  • chu2017cyclegan (строки 40–45) — статья "CycleGAN, a master of
    steganography" (Chu, Zhmoginov, Sandler 2017). Это НЕ оригинальная
    статья CycleGAN, а позднее исследование, показавшее, что CycleGAN
    скрывает информацию в выходах. Из текста главы 4 (Chapter3.tex)
    автор использует zhu2017unpaired для самого метода — что
    корректно. Но в гл. 1 (Введение, Chapter0.tex, стр. 26) ссылка
    \cite{chu2017cyclegan} означает "CycleGAN заложил основу" — это
    ошибочное цитирование. ЗАМЕНИТЬ на zhu2017unpaired в Chapter0.tex,
    стр. 26, и удалить запись chu2017cyclegan из bib (или оставить,
    если действительно ссылаются на стеганографический результат).

  • henry2021pix2pix (строки 32–39) — журнал "Research Gate Publication",
    отсутствует у реальных площадок. Авторы "Joyce Henry, Terry Natalie,
    Den Madsen" не находятся среди авторов известных работ. Скорее
    всего, это галлюцинированная или low-quality ссылка. ЗАМЕНИТЬ на
    isola2017image (которая УЖЕ ЕСТЬ в bib) во всех местах
    использования в Chapter0.tex (стр. 26).

  • Не подтверждены (требуют верификации в DBLP/Google Scholar):
    george2022wavelet, zhang2025seg, zhao2025hvt, wei2025sequential,
    bralet2024dem, zhang2024interpretable.

  • Возможно отсутствуют (нужно добавить, если используются):
    ronneberger2015u (оригинальный U-Net).
    miyato2018spectral (Spectral Normalization), если упоминается
      в гл. 5.
    Цитаты на каждую модель в сравнительной таблице §4.5.

LaTeX/ПЕЧАТЬ:
  • Дублирующий \label{subsec:lightweight_hierarchical_model}
    (Chapter4.tex строки 202 и 222) — LaTeX warning.
  • \ref{eq:cgan_loss} и \ref{eq:l1_loss} (Chapter3.tex стр. 47) — нет
    соответствующих \label. В PDF будет "??".
  • Пустой figure без \includegraphics: fig:lightweight_arch
    (Chapter4.tex стр. 206–210).
  • main.tex (стр. 28–29) подключает два файла с комментарием
    "%% Заключение" подряд (Chapter5.tex и Chapter6.tex). Это
    наследие от шаблона; пометить корректно ("Результаты" и
    "Заключение").
  • Шапка фиксирует язык таблицы содержания "russian", но в
    preambule.tex закомментирована fontenc T2A (стр. 4) — проверить,
    что Cyrillic корректно компилируется.

ИЛЛЮСТРАЦИИ:
  • Все рисунки лежат в images/ с подпапками по моделям —
    организовано хорошо.
  • images/cfrwd/.DS_Store и images/.DS_Store — служебные файлы
    macOS, можно удалить (на компиляцию не влияют).


────────────────────────────────────────────────────────────────────────────────
11. ПЛАН ДЕЙСТВИЙ ПО ПРИОРИТЕТУ
────────────────────────────────────────────────────────────────────────────────

PRIORITY 1 — БЛОКИРУЮЩИЕ (без этого работу нельзя защищать):
  □ Заменить шаблонный заголовок "Визуализация данных reactingFoam" в
    parts/Chapter5.tex.
  □ Заполнить главу 6 (Результаты): хотя бы одну сравнительную
    таблицу с метриками PSNR/SSIM/LPIPS/FID.
  □ Заполнить главу 7 (Заключение).
  □ Заполнить §5.3.2 "Лучшая собственная модель".
  □ Заменить \cite{chu2017cyclegan} → \cite{zhu2017unpaired} в
    Chapter0.tex, стр. 26.
  □ Заменить \cite{henry2021pix2pix} → \cite{isola2017image} везде.
  □ Исправить или удалить \ref{eq:cgan_loss}, \ref{eq:l1_loss} в
    Chapter3.tex стр. 47.
  □ Вставить \includegraphics в fig:lightweight_arch.

PRIORITY 2 — КРИТИЧЕСКИ ВАЖНОЕ ДЛЯ КАЧЕСТВА:
  □ Добавить ablation-таблицу для модифицированного Pix2Pix.
  □ Добавить численные результаты CFRWD-inspired модели.
  □ Добавить CFRWD-GAN в сравнительную таблицу §4.5.
  □ Добавить \cite{} для каждой модели в таблице §4.5.
  □ Заменить ошибочную ссылку на 3D U-Net (cciccek20163d) для
    канонического U-Net в Chapter3.tex стр. 29.
  □ Верифицировать ссылки 2025 года.
  □ Исправить фактические ошибки в гл. 2:
    - "лейовер" vs. угловые отражатели (городская яркость),
    - "атмосферная дымка" → "путевое рассеяние",
    - формулу R_r с оговоркой о ЛЧМ-компрессии.

PRIORITY 3 — СЖАТИЕ ПО ОБЪЁМУ (для соблюдения 60-страничного лимита):
  □ Глава 2 (физика): сжать с 7 до 3.5–4 стр.
    - объединить 4 рисунка в 1 сводный;
    - убрать дубль-списки;
    - удалить или сжать §2.4.
  □ Глава 3 (методы): сжать с 13 до 7–8 стр.
    - удалить подраздел "Дифференцируемость компонент";
    - перенести сводную таблицу метрик в Приложение;
    - сжать описание физики сенсоров (дубль с гл. 2);
    - сжать аугментации до булет-списка;
    - удалить §3.4 или ужать до 1 предложения.
  □ Глава 4 (обзор): сжать с 12 до 7 стр.
    - удалить §4.4 (трансформеры), если они не используются;
    - объединить figs U-Net + PatchGAN или удалить;
    - сжать CFRWD-блок до одной фигуры + текст;
    - убрать дубль-описания спекла/искажений (back-reference на гл. 2);
    - удалить fig:pix2pix_example.
  □ Глава 5 (вклад): объём не меняется, но содержание усиливается.
    - сжать §5.2.1 "Методологический итог" до 10 строк;
    - перенести bullet-разбор fig:epoch_270 в caption;
    - использовать освободившееся место под §5.3.2 и таблицы.

PRIORITY 4 — КОСМЕТИКА:
  □ Опечатки: "отсустствия" (Ch1), "перемещаение" (Ch2),
    "тектуры" (Ch2), "Попексельное" (Ch2), "трудности с сходимостью" (Ch3),
    "FID остаётся elevated" (Ch5).
  □ Терминологическая унификация SAR / РЛИ / РСА / радар — выбрать одно.
  □ "форшотенинг" / "лэйовер" — заменить на принятые русские термины.
  □ Удалить дублирующийся \label в Chapter4.tex.
  □ Обновить аннотацию: добавить упоминание собственных моделей и
    ConvNeXtV2.

ОЦЕНКА ВРЕМЕНИ:
  Priority 1: 8–16 часов работы (1–2 дня).
  Priority 2: 16–32 часа (если эксперименты ещё нужно прогнать).
  Priority 3: 8–12 часов (механическое сжатие).
  Priority 4: 2–4 часа.


────────────────────────────────────────────────────────────────────────────────
12. ИТОГОВЫЙ ВЕРДИКТ
────────────────────────────────────────────────────────────────────────────────

РАБОТА В ТЕКУЩЕМ ВИДЕ К ЗАЩИТЕ НЕ ГОТОВА.

Сильные стороны:
  • Хорошая структура и грамотный язык.
  • Корректная физическая часть (с минорными правками).
  • Грамотное описание метрик и функций потерь.
  • Реализован нетривиальный архитектурный вклад
    (AdaptiveFusion, независимые частотные потоки).
  • Адекватная теоретическая база и обзор литературы.

Критические недостатки:
  • Главы 6 и 7 пусты, что делает заявленный вклад недоказуемым.
  • Подраздел "Лучшая собственная модель" пуст.
  • Нет ни одной сравнительной таблицы метрик.
  • Ablation-исследования отсутствуют.
  • Несколько ошибочных или сомнительных ссылок.
  • Соотношение фон/вклад искажено в пользу фона (32 стр. фона
    против 11 стр. вклада при общем лимите 60).

Стратегический совет:
  60-страничный лимит и заполнение глав 6–7 несовместимы БЕЗ
  сжатия фоновых глав. Рекомендуемая последовательность:
    (1) Сначала жёсткое сжатие глав 2–4 на 10–13 страниц.
    (2) Параллельно — численные эксперименты и таблицы.
    (3) Заполнение глав 6 и 7 и §5.3.2 на освободившемся месте.
    (4) Финальная вычитка, исправление библиографии и LaTeX-ошибок.

При выполнении плана работа может быть доведена до состояния
"защитоспособной" в течение 2–3 недель сосредоточенной работы при
условии, что экспериментальные результаты уже посчитаны и нужно
только их оформить. Если эксперименты ещё не доведены до конца —
срок удваивается.

================================================================================
КОНЕЦ ОТЧЁТА
================================================================================
