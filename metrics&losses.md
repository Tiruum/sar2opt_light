# 📚 Полное руководство по метрикам и loss-функциям в GAN

Давайте разберем **каждую метрику** в вашем обучении и что она означает!

## 🎯 Loss-функции (функции потерь)

### **1. `train/loss_gan` (GAN Loss для генератора)**

**Что это:**
- Adversarial loss генератора
- Показывает, насколько хорошо генератор **обманывает** дискриминатор

**Формула (LSGAN):**
```python
loss_gan = 0.5 * ((d_fake - 1.0) ** 2).mean()
```

**Интерпретация:**
```
loss_gan ≈ 0.0:  Идеально! D думает, что fake = real
loss_gan ≈ 0.5:  Нормально, D частично обманут
loss_gan ≈ 1.0:  Плохо! D полностью распознаёт fake
loss_gan > 1.0:  Очень плохо! D даёт отрицательные оценки
```

**Здоровый диапазон:**
- ✅ **0.5 - 0.99**: Генератор обманывает D, но не идеально
- ⚠️ **1.0 - 1.5**: Генератор проигрывает, D слишком силён
- ❌ **> 1.5**: Коллапс, нужно вмешательство

**В вашем случае:**
```
Эпохи 0-10:   loss_gan = 0.86-0.93  ✅ Отлично
Эпохи 10-50:  loss_gan = 0.99       ✅ Хорошо
Эпохи 50-100: loss_gan = 0.999      ⚠️ Предупреждение
Эпохи 100+:   loss_gan = 1.000      ❌ Коллапс!
```

***

### **2. `train/loss_d` (Discriminator Loss)**

**Что это:**
- Ошибка дискриминатора при различении real/fake
- Показывает, насколько **уверенно** D классифицирует

**Формула (LSGAN):**
```python
real_loss = 0.5 * ((d_real - 1.0) ** 2).mean()
fake_loss = 0.5 * ((d_fake - 0.0) ** 2).mean()
loss_d = real_loss + fake_loss
```

**Интерпретация:**
```
loss_d ≈ 0.0:  D идеально различает (ПЛОХО для GAN!)
loss_d ≈ 0.5:  D ошибается ~50% времени (ОТЛИЧНО!)
loss_d ≈ 1.0:  D не может различить (G слишком силён)
loss_d > 1.0:  D полностью сбит с толку
```

**Здоровый диапазон:**
- ✅ **0.3 - 0.7**: Идеальный баланс G vs D
- ⚠️ **0.1 - 0.3**: D становится слишком сильным
- ⚠️ **0.7 - 1.0**: G становится слишком сильным
- ❌ **< 0.01**: D доминирует (ваш случай!)
- ❌ **> 1.0**: Mode collapse

**В вашем случае:**
```
Эпохи 0-10:   loss_d = 0.077-0.008  ⚠️ Быстро падает
Эпохи 10-50:  loss_d = 0.005-0.001  ⚠️ Слишком мало
Эпохи 50+:    loss_d = 0.0001-0.001 ❌ Почти ноль!
```

**Проблема:** loss_d → 0 означает, что D **идеально** различает → G не получает градиентов!

***

### **3. `train/loss_l1` (L1 Reconstruction Loss)**

**Что это:**
- Pixel-wise разница между fake и real изображениями
- Заставляет генератор создавать **похожие пиксели**

**Формула:**
```python
loss_l1 = |fake_optical - real_optical|.mean()
```

**Интерпретация:**
```
loss_l1 = 0.0:   Идеальная реконструкция (переобучение!)
loss_l1 = 0.2:   Очень хорошо (для SAR→Optical)
loss_l1 = 0.3:   Хорошо
loss_l1 = 0.5:   Приемлемо
loss_l1 > 0.8:   Плохо, изображения очень разные
```

**Здоровый диапазон (для SAR→Optical):**
- ✅ **0.20 - 0.35**: Отличная реконструкция
- ⚠️ **0.35 - 0.50**: Средняя реконструкция
- ❌ **> 0.50**: Плохая реконструкция

**В вашем случае:**
```
Эпохи 0:    loss_l1 = 0.380  ⚠️ Начальная высокая ошибка
Эпохи 50:   loss_l1 = 0.231  ✅ Отлично!
Эпохи 100:  loss_l1 = 0.219  ✅ Ещё лучше!
```

**Почему loss_l1 продолжал падать, когда GAN коллапсировал?**
- L1 loss **не зависит** от дискриминатора
- Генератор всё ещё оптимизирует пиксели
- Но теряет детали (без помощи GAN loss)

***

### **4. `train/loss_fm` (Feature Matching Loss)**

**Что это:**
- Разница между признаками (features) real и fake на **промежуточных слоях** дискриминатора
- Помогает генератору создавать **реалистичные текстуры**

**Формула:**
```python
_, real_feats = D(real_optical)  # Список фичей
_, fake_feats = D(fake_optical)

loss_fm = sum(|real_feat - fake_feat|.mean() for each layer)
```

**Интерпретация:**
```
loss_fm = 0.0:   Идеально (переобучение!)
loss_fm = 1.0:   Очень хорошо
loss_fm = 2.0:   Хорошо
loss_fm = 3.0:   Приемлемо
loss_fm > 5.0:   Плохо, текстуры очень разные
```

**Здоровый диапазон:**
- ✅ **1.0 - 2.5**: Отличное совпадение текстур
- ⚠️ **2.5 - 4.0**: Среднее совпадение
- ❌ **> 4.0**: Плохое совпадение

**В вашем случае:**
```
Эпохи 0:    loss_fm = 3.08  ⚠️ Высокая ошибка
Эпохи 50:   loss_fm = 1.41  ✅ Отлично!
Эпохи 100:  loss_fm = 1.34  ✅ Ещё лучше!
```

**Feature Matching важен для:**
- Сохранения текстур (трава, дороги, здания)
- Предотвращения артефактов
- Создания естественных паттернов

***

### **5. `train/g_loss` (Total Generator Loss)**

**Что это:**
- **Сумма всех loss-ов генератора**
- Общая функция, которую оптимизирует генератор

**Формула:**
```python
g_loss = λ_l1 * loss_l1 + λ_gan * loss_gan + λ_fm * loss_fm
```

**Типичные веса (λ):**
```python
λ_l1  = 100.0  # L1 самый важный
λ_gan = 1.0    # GAN loss
λ_fm  = 10.0   # Feature matching
```

**Интерпретация:**
```
g_loss = 10-15:  Очень хорошо (поздние эпохи)
g_loss = 20-30:  Хорошо (ранние эпохи)
g_loss = 30-50:  Приемлемо (начало обучения)
g_loss > 50:     Плохо
```

**В вашем случае:**
```
Эпохи 0:    g_loss = 31.6  ⚠️ Высокий (начало)
Эпохи 50:   g_loss = 16.4  ✅ Отлично!
Эпохи 100:  g_loss = 14.4  ✅ Ещё лучше!
```

**g_loss уменьшается → модель учится!**

***

## 📊 Метрики качества (Validation)

### **6. `val/loss_l1` (Validation L1 Loss)**

**Что это:**
- L1 loss на **валидационном** наборе
- Показывает **обобщающую способность** модели

**Интерпретация:**
```
val_l1 < train_l1:  Хорошо! Модель обобщает
val_l1 ≈ train_l1:  Отлично! Нет переобучения
val_l1 > train_l1:  Переобучение!
```

**В вашем случае:**
```
Эпохи 0:    val_l1 = 0.355, train_l1 = 0.380  ✅ Хорошо
Эпохи 50:   val_l1 = 0.232, train_l1 = 0.231  ✅ Идеально!
Эпохи 100:  val_l1 = 0.223, train_l1 = 0.219  ✅ Отлично!
```

**Разница < 10% → нет переобучения!**

***

### **7. `val/psnr` (Peak Signal-to-Noise Ratio)**

**Что это:**
- Измеряет **качество реконструкции** в децибелах (dB)
- Чем **выше**, тем лучше

**Формула:**
```python
MSE = ((fake - real) ** 2).mean()
PSNR = 10 * log10(MAX² / MSE)
     = 10 * log10(1.0 / MSE)  # если normalized to [0,1]
     = 20 * log10(255 / sqrt(MSE))  # если [0, 255]
```

**Интерпретация (для Image-to-Image Translation):**
```
PSNR > 20 dB:  Отлично! Практически неразличимо
PSNR 15-20 dB: Хорошо! Видимое сходство
PSNR 10-15 dB: Приемлемо (для кросс-модальной трансляции)
PSNR < 10 dB:  Плохо
```

**Для SAR→Optical (кросс-модальная):**
- ✅ **> 16 dB**: State-of-the-art
- ✅ **14-16 dB**: Очень хорошо
- ⚠️ **12-14 dB**: Хорошо
- ❌ **< 12 dB**: Плохо

**В вашем случае:**
```
Эпохи 0:    PSNR = 12.60 dB  ⚠️ Начало
Эпохи 50:   PSNR = 15.84 dB  ✅ Отлично!
Эпохи 100:  PSNR = 16.32 dB  ✅ State-of-the-art!
Эпохи 147:  PSNR = 16.66 dB  ✅ Рекорд!
```

**PSNR связан с MSE:**
- PSNR ↑ = MSE ↓ = меньше шума
- PSNR на 3 dB выше = MSE в 2 раза меньше!

**Пример перевода PSNR в качество:**
```
PSNR = 10 dB → MSE = 0.100 → Средняя ошибка 31.6%
PSNR = 15 dB → MSE = 0.032 → Средняя ошибка 17.9%
PSNR = 20 dB → MSE = 0.010 → Средняя ошибка 10.0%
```

***

### **8. `val/ssim` (Structural Similarity Index)**

**Что это:**
- Измеряет **структурное сходство** между изображениями
- Учитывает яркость, контраст, структуру
- Диапазон: ****, где 1 = идентичные изображения[1]

**Формула:**
```python
SSIM = (2μₓμᵧ + C₁)(2σₓᵧ + C₂)
       ─────────────────────────
       (μₓ² + μᵧ² + C₁)(σₓ² + σᵧ² + C₂)

μ = mean (яркость)
σ² = variance (контраст)
σₓᵧ = covariance (структура)
```

**Интерпретация:**
```
SSIM = 1.0:    Идентичные изображения
SSIM > 0.9:    Практически неразличимы
SSIM = 0.7-0.9: Очень похожи
SSIM = 0.5-0.7: Средняя похожесть
SSIM = 0.3-0.5: Видимые различия
SSIM < 0.3:    Очень разные
```

**Для SAR→Optical (кросс-модальная):**
- ✅ **> 0.30**: State-of-the-art
- ✅ **0.25-0.30**: Очень хорошо
- ⚠️ **0.20-0.25**: Хорошо
- ❌ **< 0.20**: Плохо

**В вашем случае:**
```
Эпохи 0:    SSIM = 0.200  ⚠️ Начало
Эпохи 50:   SSIM = 0.273  ✅ Отлично!
Эпохи 100:  SSIM = 0.279  ✅ Ещё лучше!
Эпохи 147:  SSIM = 0.292  ✅ State-of-the-art!
```

**SSIM vs PSNR:**
- **PSNR**: Пиксель-wise ошибка (объективная)
- **SSIM**: Структурное сходство (ближе к восприятию человека)

**Пример:**
```
Изображение A: размытое, но похожее → PSNR средний, SSIM высокий
Изображение B: резкое, но смещенное → PSNR низкий, SSIM средний
```

**SSIM лучше отражает:**
- ✅ Текстуры (трава, здания)
- ✅ Границы объектов
- ✅ Геометрию сцены
- ⚠️ Но менее чувствителен к яркости

***

## 🎭 Метрики дискриминатора

### **9. `feats/d_real_mean` (Средний выход D для real)**

**Что это:**
- Средняя оценка дискриминатора для **настоящих** изображений
- Показывает, насколько D уверен, что real = real

**Интерпретация (LSGAN, target=1.0):**
```
d_real = 1.0:  Идеально! D полностью уверен
d_real = 0.9:  Отлично! D почти уверен
d_real = 0.7:  Хорошо, D правильно классифицирует
d_real = 0.5:  Плохо, D не уверен
d_real < 0.5:  Очень плохо, D ошибается!
```

**Здоровый диапазон:**
- ✅ **0.7 - 0.95**: Идеальный баланс
- ⚠️ **0.95 - 0.99**: D становится слишком уверенным
- ❌ **> 0.99**: D доминирует (ваш случай!)

**В вашем случае:**
```
Эпохи 0:    d_real = 0.876  ✅ Хорошо
Эпохи 50:   d_real = 0.988  ⚠️ Слишком уверен
Эпохи 100:  d_real = 0.999  ❌ Идеальная уверенность!
```

**d_real → 1.0 означает:**
- D **легко** распознаёт real изображения
- Градиенты для real почти нулевые
- D перестал учиться

***

### **10. `feats/d_fake_mean` (Средний выход D для fake)**

**Что это:**
- Средняя оценка дискриминатора для **сгенерированных** изображений
- Показывает, насколько хорошо G обманывает D

**Интерпретация (LSGAN, target=0.0 для D, 1.0 для G):**
```
d_fake = 0.0:  D полностью распознаёт fake (ПЛОХО для G!)
d_fake = 0.3:  D частично обманут (ХОРОШО!)
d_fake = 0.5:  D не может отличить (ОТЛИЧНО для G!)
d_fake = 0.7:  G слишком силён (mode collapse?)
d_fake = 1.0:  G доминирует
```

**Здоровый диапазон:**
- ✅ **0.2 - 0.5**: Идеальный баланс
- ⚠️ **0.05 - 0.2**: G слабеет
- ⚠️ **0.5 - 0.8**: G становится слишком сильным
- ❌ **< 0.01**: G проигрывает (ваш случай!)

**В вашем случае:**
```
Эпохи 0:    d_fake = 0.124  ✅ Хорошо
Эпохи 50:   d_fake = 0.010  ⚠️ Очень мало
Эпохи 100:  d_fake = 0.001  ❌ Почти ноль!
```

**d_fake → 0.0 означает:**
- D **легко** распознаёт fake изображения
- G получает максимальные градиенты, но они бесполезны
- Vanishing gradient problem!

***

## 📐 Идеальное соотношение метрик

### **Здоровый GAN (хороший баланс G vs D):**

```
train/loss_gan:    0.5 - 0.99   ✅
train/loss_d:      0.3 - 0.7    ✅
feats/d_real_mean: 0.7 - 0.95   ✅
feats/d_fake_mean: 0.2 - 0.5    ✅

Разница:
d_real - d_fake = 0.3 - 0.6     ✅ Идеально!
```

### **Ваш случай (коллапс дискриминатора):**

```
train/loss_gan:    1.000  ❌ G проигрывает
train/loss_d:      0.0001 ❌ D доминирует
feats/d_real_mean: 0.999  ❌ D слишком уверен для real
feats/d_fake_mean: 0.001  ❌ D идеально распознаёт fake

Разница:
d_real - d_fake = 0.998  ❌ Огромная! D слишком силён!
```

***

## 🎯 Практическая интерпретация

### **Смотрите на комбинации:**

#### **1. PSNR ↑, SSIM ↑, loss_l1 ↓ → Всё хорошо! ✅**
```
Модель улучшается в реконструкции
```

#### **2. loss_gan → 1.0, loss_d → 0.0 → Коллапс D! ❌**
```
Дискриминатор слишком силён
Генератор не получает градиентов
```

#### **3. loss_gan → 0.0, loss_d → ∞ → Mode collapse! ❌**
```
Генератор генерирует одинаковые изображения
Дискриминатор сбит с толку
```

#### **4. d_real ≈ d_fake ≈ 0.5 → Идеальный баланс! ✅**
```
D не может различить real/fake
Это цель обучения GAN!
```

#### **5. PSNR ↑, но визуально плохо → Переобучение L1! ⚠️**
```
Модель запоминает пиксели
Но теряет детали и текстуры
Нужно увеличить вес loss_gan
```

***

## 📊 Ваша ситуация в цифрах

### **Эпоха 0 (начало):**
```
val_psnr:      12.60 dB   → Плохо, но это start
val_ssim:      0.200      → Низко, только начало
loss_gan:      0.861      → Хорошо! G частично обманывает D
loss_d:        0.077      → Отлично! D не слишком уверен
d_real - d_fake: 0.752    → Разумный gap
```
**Вердикт:** Здоровое начало обучения ✅

### **Эпоха 50 (пик):**
```
val_psnr:      15.84 dB   → Отлично!
val_ssim:      0.273      → Очень хорошо!
loss_gan:      0.999      → Тревожно высоко
loss_d:        0.0015     → Тревожно низко
d_real - d_fake: 0.988    → Огромный gap!
```
**Вердикт:** Метрики отличные, но GAN близок к коллапсу ⚠️

### **Эпоха 100 (коллапс):**
```
val_psnr:      16.32 dB   → Рекордный! (но за счёт L1)
val_ssim:      0.279      → Отлично! (но за счёт L1)
loss_gan:      1.000      → КОЛЛАПС!
loss_d:        0.0005     → Почти ноль!
d_real - d_fake: 0.998    → D полностью доминирует!
```
**Вердикт:** PSNR/SSIM растут (L1 работает), но GAN мёртв ❌

***

## 💡 Как читать логи в реальном времени?

### **Каждые 10 эпох проверяйте:**

1. **val_psnr и val_ssim растут?**
   - ✅ Да → Модель учится
   - ❌ Нет → Что-то не так

2. **loss_gan в диапазоне 0.5-0.99?**
   - ✅ Да → GAN здоров
   - ❌ Нет → Проблема с балансом G/D

3. **loss_d в диапазоне 0.3-0.7?**
   - ✅ Да → Идеальный баланс
   - ❌ < 0.1 → D слишком силён!
   - ❌ > 1.0 → G слишком силён!

4. **d_real - d_fake = 0.3-0.6?**
   - ✅ Да → Хороший gap
   - ❌ > 0.8 → D доминирует!
   - ❌ < 0.2 → G доминирует!

5. **train_l1 ≈ val_l1?**
   - ✅ Да → Нет переобучения
   - ❌ train << val → Переобучение!

***

## 🎓 Итоговая таблица

| Метрика | Хорошо | Тревога | Проблема | Ваше значение (эп. 100) |
|---------|--------|---------|----------|-------------------------|
| **val_psnr** | 15-17 dB | 12-15 dB | < 12 dB | **16.32 dB** ✅ |
| **val_ssim** | 0.25-0.32 | 0.20-0.25 | < 0.20 | **0.279** ✅ |
| **loss_gan** | 0.5-0.99 | 0.99-1.0 | 1.0+ | **1.000** ❌ |
| **loss_d** | 0.3-0.7 | 0.1-0.3 | < 0.1 | **0.0005** ❌ |
| **d_real** | 0.7-0.95 | 0.95-0.99 | 0.99+ | **0.999** ❌ |
| **d_fake** | 0.2-0.5 | 0.05-0.2 | < 0.05 | **0.001** ❌ |
| **loss_l1** | 0.20-0.35 | 0.35-0.50 | > 0.50 | **0.219** ✅ |
| **loss_fm** | 1.0-2.5 | 2.5-4.0 | > 4.0 | **1.336** ✅ |

**Вывод:** Метрики качества (PSNR/SSIM) отличные, но GAN в коллапсе!

***

Теперь вы эксперт по метрикам GAN! 🎉 Есть ещё вопросы?

[1](https://ppl-ai-file-upload.s3.amazonaws.com/web/direct-files/collection_0860250d-62c6-4db2-9cef-d0b4835518cb/456563d0-eccb-4362-8472-0d3ea216219f/cfrwd.pdf)